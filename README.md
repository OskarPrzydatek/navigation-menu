# Navigation Menu

## Getting Started

To run the project at first we need to install all needed dependencies

```bash
npm install
```

Secondly, run the development server:

```bash
npm run dev
```

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
